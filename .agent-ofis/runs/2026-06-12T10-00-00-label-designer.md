---
title: "Görsel etiket tasarımcısı + Stok/Belge menü ayrımı + CORS PATCH/DELETE bug fix"
type: run
status: completed
date: 2026-06-12
run_id: 2026-06-12T10-00-00-label-designer
manager: Tech Lead
crew: [[crew/backend-engineer]], [[crew/frontend-engineer]], [[crew/qa-engineer]]
topics: [ui, ux, etiket, label, tasarim, designer, menu, cors, bug, schema]
tags: [run/completed, ui, backend, frontend, bugfix]
related_runs: [[runs/2026-06-12T09-05-00-ui-cari-alan]]
aliases: ["label-designer", "etiket-tasarimci", "cors-fix"]
---

# Run — Etiket tasarımcısı + menü + CORS fix

## Yapılan
1. **Menü ayrımı** — 'Stok & Belge' → ikiye: **Belge & İş Emri** (işin temeli) önce, **Stok (Durum)** (rapor niteliği) ayrı.
2. **Barkod** — 'Barkod Tipi (Parse)' → **'Barkod Tipleri'**.
3. **Görsel etiket tasarımcısı** (kullanıcı seçimi: sürükle-bırak):
   - **Şema**: `TBLLABELTYPE.layoutJson` (Text) — boyut + element listesi JSON. *(bizim eklediğimiz; legacy Excel'de yok, görsel tasarımcı için zorunlu)*. Migration `label_layout_json` (diff+deploy).
   - **Backend**: factory'e `GET /:id` + `label` şemasına `layoutJson`.
   - **Frontend** `LabelDesigner.tsx`: mm ölçekli tuval, **metin / veri-alanı / barkod / çizgi** elemanları, fare ile **sürükle**, sağ **özellik paneli** (konum/boyut/font/kalın/hiza, alan bağlama), **örnek veriyle canlı önizleme**, temsili barkod çizimi, **Yazdır/PDF**. `formConfig` label-types; GenericList **'Tasarla'**; App route `label-types/:id/design`.

## 🐞 BONUS bug (önemli)
`@fastify/cors { origin: true }` tek başına allow-methods'u **GET,HEAD,POST**'a kısıtlıyormuş → tarayıcıdan **TÜM PATCH (düzenleme) ve DELETE (silme)** preflight'ta patlıyordu. Smoke `app.inject` (server-side) olduğu için kaçmış. `methods: [GET,HEAD,POST,PUT,PATCH,DELETE,OPTIONS]` eklendi → düzeldi. Tarayıcıdan PATCH 200 + DELETE 200 doğrulandı.

## Doğrulama
- backend tsc ✅ · migrate deploy ✅ · CORS preflight (PATCH/DELETE açık) ✅ · designer E2E (oluştur→tasarla→kaydet→reload: 5 element persist) ✅ · tarayıcı DELETE 200 ✅ · web tsc/build ✅ · AntD6 deprecation (addonBefore→prefix, Space direction→orientation) temiz ✅ · smoke ✅

## Karar
- Etiket tasarımı tek kolonda (`layoutJson`). Sürükle-bırak yaklaşımı.

## Sonraki
- Temsili barkod → gerçek Code128 (yazdırmada) · Belge satırından gerçek veriyle etiket bas · StokBar parite (görsel)
