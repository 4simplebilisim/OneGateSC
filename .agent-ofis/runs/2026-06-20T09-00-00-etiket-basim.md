---
title: "Etiket Basım (etiket tipini kullanan dinamik form → Code128 yazdırma)"
type: run
date: 2026-06-20
topics: [etiket, label, basim, print, barcode, ui]
status: completed
tags: [#run/completed, #ui]
aliases: [etiket-basim, label-print]
---

# Etiket Basım — "tamamla ekranı"

İstek: "tamamla ekranı" → [[2026-06-20T08-15-00-dinamik-etiket-tipi]] ile kurulan **Etiket Tipi**'nin gerçek KULLANIM/BASIM akışını tamamla (item'lar dinamik form olsun, doldurulup Code128 barkodlu etiket bassın).

## Yapıldı
- **[[web/src/pages/LabelPrint.tsx]]** (yeni ekran, `label-templates/:id/print`):
  - Etiket tipi + item'larını yükler; `isVisible` filtreler, `sortOrder` sıralar; `defaultValue` ile alanları ön-doldurur.
  - **itemType → kontrol** eşlemesi: NUMBER→InputNumber, DATE→DatePicker, CHECKBOX→Switch, BARCODE/TEXT/COMBO/LOOKUP→Input (COMBO/LOOKUP'ta "sorgu kaynağı yakında" placeholder — güvenli SQL-runner sonraki adım).
  - **Etiket Bas**: zorunlu-alan kontrolü → BARCODE item'ından (yoksa ilk item) barkod değeri → `code128Svg` ile yazdırma penceresi (item satırları + barkod, `@media print`).
- **[[web/src/App.tsx]]**: `label-templates/:id/print` route (LabelPrint import).
- **[[web/src/pages/GenericList.tsx]]**: `label-templates` listesine "Bas" butonu (PrinterOutlined) → print ekranı.

## Doğrulama
- web tsc ✓ · smoke PASSED ✓
- E2E + UI (LTID=3, "Mal Kabul Etiket"; Barkod*/Miktar(vars=1)/SKT item'ları):
  - Render: başlık + Alanlar (3) + her item doğru kontrol; Miktar `defaultValue=1` ön-dolu ✓
  - Zorunlu kontrol: boş Barkod ile "Bas" → uyarı, **print penceresi açılmadı** ✓
  - Basım HTML (window.open yakalandı): 3 satır (Barkod/Miktar/SKT), Miktar=1, **Code128 `<svg>`** + barkod değeri "ABC-12345" + etiket adı ✓
  - 0 konsol hatası. Test verisi temizlendi (0 şablon kaldı).
- NOT: API curl testinde Türkçe "ü" başlık → `FST_ERR_CTP_INVALID_CONTENT_LENGTH` (Git-Bash/curl Content-Length yanlış sayıyor; tarayıcı doğru gönderir — API bug'ı değil). ASCII başlıkla doğrulandı.

## Kalan
- COMBO/LOOKUP item'larının sorgu-güdümlü kaynağı (TBLLABELTEMPLATEQUERY SQL'i çalıştıran güvenli runner) — şimdilik metin girişi.
- Gerçek yazdırma item alanlarının (font/x-y/genişlik) tasarım yerleşimi — şu an basit dikey liste + barkod.
