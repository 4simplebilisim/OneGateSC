---
title: "Giriş/çıkış koşulu tipleri + yönlendirme (directed putaway) — çekirdek 4 tablo"
type: run
status: completed
date: 2026-06-09
run_id: 2026-06-09T15-25-00-routing
manager: Tech Lead
topics: [wms, kosul, yonlendirme, directed-putaway, routing]
tags: [run/completed, db, wms]
related_runs: [[runs/2026-06-09T15-10-00-txncreate]]
---

# Run — Koşul tipleri + yönlendirme (directed putaway)

Kullanıcı onayı: **çekirdek 4 tablo** (büyük ~26 tablolu alt-sistemden). Legacy birebir.

## Yapıldı
- 💾 Database — `TBLENTRYCONDITIONTYPE` (giriş koşul tipi), `TBLEXITCONDITIONTYPE` (çıkış koşul tipi), `TBLROUTINGTYPE` (yönlendirme tipi), `TBLROUTINGRULE` (ürün/grup → lokasyon/grup kuralı) + MaterialLinkType/LocationLinkType enum. Migration `routing_conditions` (20). 46 tablo.
- ⚡ Backend — `routing.ts` servisi: `suggestPutawayLocations(companyId, productId)` directed putaway (ürün doğrudan veya grubu üzerinden eşleşir; hedef tek lokasyon veya grup üyeleri; öncelik sıralı, tekilleştirilmiş). Route: `/api/routing-rules` CRUD + `GET /api/routing-rules/suggest?productId=`. 3 tip master factory ile (`/api/{entry,exit}-condition-types`, `/api/routing-types`). Seed: STD yönlendirme + PRD001→R01-01 örnek kural.
- 🎨 Frontend — "Koşul/Yönlendirme" menü grubu (4 ekran); 3 tip master'ı form'lu (code+açıklama), kurallar listeli.
- 🔍 QA — Routing E2E 7/7 (suggest seed kuralı, 2. kural→2 lokasyon priority sıralı, olmayan ürün→boş, VIEWER 403, tip master create); smoke yeşil (+suggest endpoint).

## Doğrulama
- typecheck ✅ · migrate deploy ✅ (20 migration) · seed ✅ · Routing E2E 7/7 ✅ · smoke ✅ · UI build ✅
- 46 domain tablo.

## Not (kapsam)
Alt-sistemin kalan ~22 tablosu (parametre/log/kontrol sahası/kırma nedeni-şifresi) bilinçli atlandı — kullanıcı "çekirdek 4" dedi.

## Sonraki (öneri)
- [ ] Mal kabulde suggest entegrasyonu (belge satırına önerilen lokasyon)
- [ ] UI: routing-rule oluşturma formu (koşullu ref select)
