---
title: "Koyu mod — darkAlgorithm + semantik değişkenler + toggle"
type: run
status: completed
date: 2026-06-12
run_id: 2026-06-12T08-15-00-ui-dark-mode
manager: Tech Lead
crew: [[crew/frontend-engineer]]
topics: [ui, ux, arayuz, frontend, tasarim, theme, koyu-mod, dark-mode]
tags: [run/completed, ui, frontend]
related_runs: [[runs/2026-06-12T07-45-00-ui-remaining-screens]]
aliases: ["dark-mode", "koyu-mod"]
---

# Run — Koyu mod

## Yapılan (kullanıcı: "koyu mod")
- **theme.ts → `makeTheme(mode)`** — AntD `darkAlgorithm`, marka primary korunur; koyu token seti (colorBgLayout `#0E1626`, container `#141E33`, elevated `#1A2740`, koyu border/metin) + per-komponent koyu değerleri (Table/Card/Tag/Segmented/Form/Descriptions).
- **`themeMode.tsx`** (YENİ) — `ThemeModeProvider` modu `localStorage('og_theme')`'de tutar, `<html data-theme>` set eder, `ConfigProvider`+`AntApp`'i sarar; `useThemeMode()` hook.
- **Shell** — header'a ay/güneş toggle; `Layout.Content` bg `var(--og-page-bg)`.
- **styles.css** — tüm sabit yüzey hex'leri **semantik CSS değişkenlerine** (`--og-page-bg / sunken / border-soft / table-head / muted / ink / formbar`); `:root` açık, `[data-theme=dark]` koyu. Tek yerde döner.
- **Ekranlar** — inline yüzey hex'leri var()'a (Dashboard stat değeri, TxnCreate/DocumentCreate satır mini-kartı `og-linecard`, LocationBulkGenerate matris).
- **Login** — bilinçli **her zaman açık** (kendi light `ConfigProvider`'ı) — markalı gradient panel.
- **AntD6 temizliği** — Space `split`→`separator`, Divider kaldırıldı.

## Doğrulama
- web tsc ✅ · web build ✅ (Login dahil) · **canlı DOM** ✅ (dark: body #0E1626, card #141E33, stat/başlık açık metin, switch #172238, formbar koyu, tablo başlığı #172238; **toggle + localStorage kalıcı**; Login dark seçiliyken bile açık) · smoke ✅

## Karar
- Yüzey renkleri **semantik CSS değişkenlerinden** gelir (açık/koyu tek noktada). Login markalı → koyu moddan muaf.

## Sonraki
- Sistem teması otomatik (`prefers-color-scheme`) — istenirse · UI dışı: demo son prova / niş flag'ler
