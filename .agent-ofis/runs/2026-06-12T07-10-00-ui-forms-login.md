---
title: "UI redesign 2. dalga — Login + form ekranları (StokBar deseni)"
type: run
status: completed
date: 2026-06-12
run_id: 2026-06-12T07-10-00-ui-forms-login
manager: Tech Lead
crew: [[crew/frontend-engineer]]
topics: [ui, ux, arayuz, frontend, tasarim, form, login, theme]
tags: [run/completed, ui, frontend]
related_runs: [[runs/2026-06-12T06-15-00-ui-redesign]]
---

# Run — UI redesign 2. dalga (Login + formlar)

## Yapılan (kullanıcı: "1-2-3")
1. **Login** — dekoratif orb'lara `og-float` yumuşak yüzme animasyonu (transform-only, 60fps); marka wordmark `og-display` (Plus Jakarta 800); büyük başlık zaten display fonta bindi.
2. **GenericForm** — dar tek-kolon karttan **2-kolon responsive grid** (`Row/Col`) + `og-section-card` bölüm kartı + boolean'lar **`og-switchrow`** toggle satırı + **sticky cam-efektli kaydet çubuğu** (`og-formbar`) + `PageHeader`.
3. **OperationTypeForm** — 5 bölümlü yapı korundu, aynı desene taşındı: 26 switch satırı, lg'de 3-kolon grid, sticky bar, başlık+altyazı ("operasyon tanımı işin kalbi").

Ortak stiller → `styles.css`: `.og-formbar` (sticky/blur), `.og-switchrow` (hover'lı toggle), `.og-section-card` (gradient başlık). Hepsi `theme.ts` tokenından beslenir — **yeni inline hex yok**.

## Doğrulama
- web tsc ✅ · web build ✅ · **canlı DOM** ✅ (Login h1 Plus Jakarta + 2 orb float; GenericForm başlık+sticky bar+grid; OperationTypeForm 5 bölüm + 26 switch satırı; konsol hatasız) · smoke ✅

## Karar
- **Standart form deseni**: `og-page` + `PageHeader` + `og-section-card` + `og-switchrow` + `og-formbar`. Tüm gelecek formlar bunu izleyecek.

## Sonraki
- TxnCreate / DocumentCreate / LocationBulkGenerate aynı desene · GenericDetail cila · StokBar ekran-başı birebir parite
