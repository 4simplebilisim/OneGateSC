---
title: "Modül derinleştirme — CRUD/pagination + stok kartı + sipariş düzenleme + raporlar"
type: run
status: completed
date: 2026-06-09
run_id: 2026-06-09T05-56-00-deep
manager: Tech Lead
topics: [pagination, crud, stok-karti, rapor, siparis, analitik]
tags: [run/completed, api, derinlestirme]
---

# Run — Modül derinleştirme (4 alan)

**İstek:** "ai beklesin, modülleri derinleştirelim" → kullanıcı 4 alanı da seçti (CRUD/pagination + stok derinliği + sipariş derinliği + raporlama).

## Yapıldı
- ⚡ Backend — ① `pagination.ts` (parsePagination/paginated); products/stock/purchase-orders/sales-orders listeleri **sayfalı** ({data,total,page,pageSize,pageCount}); **PATCH update** products/warehouses/units/partners; products list **arama** (code/name).
- ⚡ Backend — ② **Stok kartı** `GET /api/stock/card?productId=` — ürünün tüm hareketleri (belge satırları, in/out yön).
- ⚡ Backend — ③ **Sipariş düzenleme** PATCH /api/purchase-orders/:id + /api/sales-orders/:id (DRAFT, satır değiştir + orderFinance ile toplam yeniden hesap).
- ⚡ Backend — ④ **Raporlar** `/api/reports`: stock-summary (groupBy), open-orders (kalan miktar), invoice-aging (açık/vadesi geçen), mrp-summary.
- 🔍 QA — Derinleştirme E2E 10/10; smoke pagination'a uyarlandı + rapor/kart kontrolleri; yeşil.

## Doğrulama
- typecheck ✅ · seed ✅ · smoke ✅ · Deep E2E 10/10 ✅
- ~95 endpoint (13 route + reports).

## Not
- Şema değişmedi (migration 12'de sabit). Pagination yanıtı `{data,...}` zarfı; smoke 4 endpoint için `.data` kontrol ediyor.

## Sonraki
- [ ] Pagination'ı kalan listelere yay (documents/invoices/vehicles/shipments)
- [ ] Cari/stok hareket ledger kalıcı tablo (şu an belge satırından türetiliyor)
- [ ] AI modülü (en sonda)
